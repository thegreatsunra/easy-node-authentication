// const path = require('path')

// const app = require(path.resolve(__dirname, '../../../../', 'src/server'))

/* eslint-disable no-undef */
describe('App', () => {
  it('should compare true and true', () => {
    expect(true).toEqual(true)
  })
})
/* eslint-enable */
